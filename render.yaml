services:
  - type: web
    runtime: docker
    name: pganalyze-collector
    dockerCommand: collector --statefile=/app/state.out --no-log-timestamps
    plan: starter
    region: oregon
    envVars:
      - key: PGA_API_KEY
        sync: false